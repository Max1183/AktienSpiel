function NavigationButton({ children, active }) {
    return <button className="list-group-item list-group-item-action list-group-item-success text-center">
        {children}
    </button>
}

export default NavigationButton
